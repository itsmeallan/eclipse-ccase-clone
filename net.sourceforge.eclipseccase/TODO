ToDos
=====

For future:

- Investigate the CCRC interface.
- Supporting the Eclipse 3.4

2.1.7
-----

Bug release.

2.1.6
-----

Bug release.


2.1.5
-----

Bug release.

2.1.4
-----

- Continue fixing bugs.
- Enhance exception handling and error checking with clearcase-java and eclipseccase layers.
- Add fix for uploading .zip to sourceforge.net
- New refresh.

1.0.0 
-----
(must be ready with Eclipse 3.0)

  - general
    o (postponed) investigate new Eclipse API
    
  - repository provider
    x (done) background jobs
    x (done) scheduling rules (IMoveDeleteHook, auto-checkout ...)
  
  - ui
    x (done) finanlize "checkouts view"
    o (postponed) finanlize recursive actions (eg. check for new when checkin folder)
    x (done) rework auto-checkout to provide UI feedback instead of exceptions
    x (done) support native ClearCase UI via "cleardlg" executable

  - clearcase
    x (done) API for detecting if a resource is checked out by someone else

1.1.0
-----

  - finanlize recursive actions (eg. check for new when checkin folder)
  - "compare with" support
  - investigate new Eclipse API / new workflow (research)
  
  
long term goals
---------------
  
  - performance improvements using JNI
  - evolve ClearCase Java API (possibly UCM and managing views)
